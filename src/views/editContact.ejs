<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Kontak</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>

    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="<%= url %>">Puong206</a>
        </nav>
        <h1>Edit Kontak</h1>
        <form action="<%= url + 'contact/update/' + contact.id %>" method="POST" class="glass-effect card">
            <label for="name">Name:</label>
            <input type="text" name="name" id="name" value="<%= contact.name %>" class="form-control" required>
            <br>

            <label for="email">Email:</label>
            <input type="email" name="email" id="email" value="<%= contact.email %>" class="form-control" required>
            <br>

            <label for="phone">Phone:</label>
            <input type="text" name="phone" id="phone" value="<%= contact.phone %>" class="form-control" required>
            <br>

            <label for="address">Address:</label>
            <input type="text" name="address" id="address" value="<%= contact.address %>" class="form-control" required>
            <br>

            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </body>

</html>